[Unit]
Description=Droid mount for /odm
Before=local-fs.target systemd-modules-load.service

[Mount]
What=/dev/mmcblk0p67
Where=/odm
Type=ext4
Options=ro,barrier=1
# Options had SELinux context option:  

# Default is 90 which makes mount period too long in case of
# errors so drop it down a notch.
TimeoutSec=10

[Install]
WantedBy=local-fs.target

# From ./out/target/product/kirin/vendor/etc/fstab.kirin :
# /dev/mmcblk0p67          /odm         ext4    ro,barrier=1                                                  wait,recoveryonly,slotselect
